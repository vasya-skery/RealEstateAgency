<div id="catalog">
  <div class="container">
    <div class="row">
      <div class="col-12">
        <h2>Catalog</h2>

        <p>CoderExample is an example library of quality design/development resources offered for free to the
          community.
          Here you’ll find downloadable code files, details demonstration, live demo and also various support through
          comments.</p>

        <h2>Meet the Codie</h2>

        <p>Hello Guys! My name is Arka and I’m a developer from Kolkata, India. I’m loved to work with latest
          technologies
          though I have some experience on working Php & Javascript related technologies. Nowadays I’m lost in Laravel
        </p>



        <p>This site is all about sharing my passion and knowledge with you. So feel free to look around and you might
          find
          useful stuff for your projects.</p>

        <p>If want to dirty your hands for some contribution then contact me. Don’t hesitate to get in touch. I’d be
          happy
          to feature your work!</p>

        <p>Keep up to date with new post by subscribing to the Email Alerts or follow CoderExample on <a href="https://fb.com/arkaindas">Facebook</a>
          / <a href="https://twitter.com/arkaindas">Twitter</a></p>
      </div>
    </div>
    <div class="row">
      <div class="col-12">
        <div class="catalog-filter">
          <form action="">
            <select class="js-example-basic-multiple" id="mySelect2" name="states[]" style="width: 100%" multiple="multiple">
            </select>
          </form>
        </div>
      </div>
    </div>
    <div class="row" id="objects-list">
    </div>
  </div>
</div>

<script>
  // Set up the Select2 control



  $(document).ready(function() {


  

    // get Data
  $.ajax({
    url: "data/data.json"
  }).done(function(data) {
    // var json = JSON.parse(data);
    console.log(data);
    var source = document.getElementById("select2-template").innerHTML;
    var template = Handlebars.compile(source);
    var html = template(data);
    $("#mySelect2").append(html);
    
  });

    // get Data catalog list
  $.ajax({
    url: "data/data.json"
  }).done(function(data) {
    // var json = JSON.parse(data);
    console.log(data);
    var source = document.getElementById("objects-list-template").innerHTML;
    var template = Handlebars.compile(source);
    var html = template(data);
    $("#objects-list").append(html);
  });


  $('#mySelect2').select2({
    width: 'resolve'
  });

  setTimeout(function() {
    // init slider
    $(".card-img-slider").slick();
  }, 400);
});
</script>