<div class="container">
  <div class="row">
    <div class="col-12">
      {{!-- {{#each object}} --}}
<article>
  <section>
    <h2>{{items.object.title}}123</h2>
    <img src="{{object.cover}}"/>
    <p>{{object.excerpt}}</p>
  </section>
</article>
{{!-- {{/each}} --}}
    </div>
  </div>
</div>